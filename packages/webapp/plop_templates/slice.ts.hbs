/*!
 * Copyright (c) Microsoft. All rights reserved.
 * Licensed under the MIT license. See LICENSE file in the project.
 */
import { createSlice } from '@reduxjs/toolkit'
import { RootState } from '~store'

export const slice = createSlice({
	name: '{{camelCase name}}',
	initialState: {
		isLoading: false,
		data: fakeNavigators
	},
	reducers: {
		set{{properCase name}}: (state, action) => {
			state.data = action.payload
		}
	}
})

/**
 * Exported Actions
 */
export const { set{{properCase name}} } = slice.actions

/**
 * Private Actions
 */
const { setLoading } = slice.actions

/**
 * Add async and dynamic actions here
 */
export const load{{properCase name}} = (state: RootState) => state.{{camelCase name}}
/**
 * Add getters here
 */
export const get{{properCase name}} = (state: RootState) => state.{{camelCase name}}

// Export reducer
export default slice.reducer
